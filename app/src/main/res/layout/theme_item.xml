<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    >

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/theme_btn_img"
        android:layout_width="93dp"
        android:layout_height="108dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@color/primary_100"
        app:shapeAppearanceOverlay="@style/ThemeRoundedCorners"
        app:strokeWidth="1dp"
        app:strokeColor="@color/unselected"
        android:padding="1dp"
        />

    <TextView
        android:id="@+id/theme_btn_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="테마명"
        android:textColor="#817F7C"
        android:textSize="20sp"
        android:fontFamily="@font/nanum_neuris"
        app:layout_constraintBottom_toBottomOf="@+id/theme_btn_img"
        app:layout_constraintEnd_toEndOf="@+id/theme_btn_img"
        app:layout_constraintStart_toStartOf="@+id/theme_btn_img"
        app:layout_constraintTop_toTopOf="@+id/theme_btn_img" />

</androidx.constraintlayout.widget.ConstraintLayout>